---
name: git-commit
description: Analyzes staged changes and writes a git commit message. Use when you need to generate a commit message.
allowed-tools: Bash(gh *), Bash(git diff *), Bash(git status *), Bash(git log *), Bash(git show *)
---

Please review the staged changes and write a git commit message.
Only include quantitative improvement details (such as "reduced X lines of code" or "improved performance by X%") when they are truly necessary.
